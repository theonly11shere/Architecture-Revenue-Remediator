{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -c \"import uvicorn, os; uvicorn.run('app.api.api:app', host='0.0.0.0', port=int(os.environ.get('PORT', 8000)))\""
  }
}